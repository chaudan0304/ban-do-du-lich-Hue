"""
=============================================================================
tests/__init__.py - Package khởi tạo cho Tests (Tests Package Init)
tests/__init__.py - Tests Package Init
=============================================================================
Mô tả / Description:
    Package rỗng — đánh dấu thư mục `tests/` là Python package.
    Empty package — marks the `tests/` directory as a Python package.

Cấu trúc / Structure:
    tests/
    ├── __init__.py        ← File này / This file
    ├── run_all_tests.py   → Test runner (chạy tất cả test / runs all tests)
    ├── test_auth.py       → Test xác thực (register, login, profile)
    ├── test_planner.py    → Test AI planner (generate itinerary)
    └── test_recommend.py  → Test gợi ý AI (cold-start, PageRank, exclusion)
=============================================================================
"""
